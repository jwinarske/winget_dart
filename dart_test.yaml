tags:
  integration:
    # Integration tests require a live Windows environment with WinGet.
    # Run with: dart test --tags=integration
    # Skipped by default in: dart test --exclude-tags=integration
