tags:
  integration:
    # Integration tests require a live Windows environment with WinGet.
    # Run explicitly with: dart test --tags=integration --run-skipped
    skip: "Requires live Windows + WinGet (run with --tags=integration --run-skipped)"
