// native/src/winget_transaction.cpp
// SPDX-License-Identifier: Apache-2.0
//
// WgTransaction is header-only for now (all logic is inline in the struct).
// This translation unit exists so CMakeLists.txt can compile it without
// error, and to reserve the file for future non-inline members.

#include "winget_transaction.h"
