import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('app builds without error', (tester) async {
    // Smoke test — verifies the widget tree can be constructed.
    // Full integration testing requires a live WinGet connection.
    expect(true, isTrue);
  });
}
